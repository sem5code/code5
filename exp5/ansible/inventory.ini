[appservers]
# Replace the example below with your target host(s).
# Example using SSH:
# myserver ansible_host=203.0.113.10 ansible_user=ubuntu

# For quick local testing you can use the control machine itself:
localhost ansible_connection=local
